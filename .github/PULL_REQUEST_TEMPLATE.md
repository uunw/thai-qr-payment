## Summary

<!-- One or two lines: what does this change and why. -->

## Changes

<!-- Bullet list of user-visible changes. -->

## Testing

- [ ] `pnpm test` passes locally
- [ ] `pnpm exec oxlint packages/*/src` clean
- [ ] `pnpm build` produces ESM + CJS output for every package
- [ ] If the change touches the wire payload or QR encoder, the EMVCo CRC round-trip test still passes

## Changeset

- [ ] `pnpm changeset` recorded if this PR ships a user-visible change
