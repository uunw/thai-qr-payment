# Changelog

Per-package changelogs live next to each package (e.g. `packages/payload/CHANGELOG.md`). This top-level file is reserved for monorepo-wide events (tooling migrations, repo-level breaking changes).

## Unreleased

- Initial public release scaffolding.

# CI metadata test
